/**
 * Loader component — re-exports LoadingSpinner and PageSpinner for consistency
 * with the requested component name (Loader.jsx).
 */
import LoadingSpinner, { PageSpinner } from './LoadingSpinner'

export { PageSpinner }
export default LoadingSpinner
