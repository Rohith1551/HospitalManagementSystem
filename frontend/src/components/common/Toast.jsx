/**
 * Toast notifications are handled by react-hot-toast in main.jsx (Toaster).
 * This module re-exports the useToast hook for consistent usage.
 * Use: import { useToast } from '../hooks/useToast' or toast from 'react-hot-toast'.
 */
export { useToast } from '../../hooks/useToast'
